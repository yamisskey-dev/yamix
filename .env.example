# ===========================================
# Yamix Production Environment Variables
# ===========================================
# Copy this file to .env and fill in the values
# cp .env.example .env

# ----- Application -----
# Public URL of your Yamix instance
WEB_URL=https://your-domain.com

# Server binding (for nginx reverse proxy)
YAMIX_HOST=127.0.0.1
YAMIX_PORT=3026

# Database password
POSTGRES_PASSWORD=change-this-to-a-secure-password

# ----- Required Secrets -----
# OpenAI API Key (required for AI counseling)
OPENAI_API_KEY=sk-your-openai-api-key

# JWT Secret (use a random string, e.g.: openssl rand -base64 32)
JWT_SECRET=change-this-to-a-secure-random-string

# Yamii API Key (shared secret between Yamix and Yamii)
# Generate: openssl rand -hex 32
YAMII_API_KEY=change-this-to-a-secure-random-string

# ----- Optional (Production) -----
# Message encryption key (if not set, derived from JWT_SECRET)
# MESSAGE_ENCRYPTION_KEY=change-this-to-a-secure-random-string
